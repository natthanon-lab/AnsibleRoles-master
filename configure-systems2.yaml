# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: ubuntu
  become: yes

  tasks:

- name: Call Roles
  hosts: ubuntu
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
